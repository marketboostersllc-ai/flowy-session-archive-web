# Sin estas dos variables, la web sirve datos de ejemplo (deterministas) en
# vez de sesiones reales. Son la URL y la clave PUBLICA (anon) del proyecto
# de Supabase "flowy-session-archive" — seguro exponerlas al cliente, las
# tablas son de solo lectura pública por RLS.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
